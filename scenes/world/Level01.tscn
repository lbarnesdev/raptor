[gd_scene load_steps=25 format=3 uid="uid://raptor0level001"]

; ── External resources ────────────────────────────────────────────────────────
[ext_resource type="Script" path="res://src/World/ScrollCamera.cs" id="1_scrollcam"]
[ext_resource type="PackedScene" uid="uid://raptor0player01" path="res://scenes/player/Player.tscn" id="2_player"]
[ext_resource type="Script" path="res://src/Projectiles/ProjectilePool.cs" id="3_pool"]
[ext_resource type="PackedScene" uid="uid://raptor0bolt01" path="res://scenes/projectiles/PlasmaBolt.tscn" id="4_plasmabolt"]
[ext_resource type="PackedScene" uid="uid://raptor0hud01" path="res://scenes/ui/HUD.tscn" id="5_hud"]
[ext_resource type="PackedScene" uid="uid://raptor0enemy01" path="res://scenes/enemies/BasicEnemy.tscn" id="6_basicenemy"]
[ext_resource type="Script" path="res://src/World/LevelDirector.cs" id="7_leveldirector"]
[ext_resource type="PackedScene" uid="uid://raptor0boss01" path="res://scenes/boss/Boss.tscn" id="8_boss"]
; id 9 (BossHealthBar.cs) removed — boss bar now lives inside HUD.tscn (TICKET-704)
[ext_resource type="PackedScene" uid="uid://raptor0cblast01" path="res://scenes/projectiles/ConstitutionBlast.tscn" id="10_cblast"]
[ext_resource type="PackedScene" uid="uid://raptor0foxpulse01" path="res://scenes/projectiles/FoxBroadcastPulse.tscn" id="11_foxpulse"]
[ext_resource type="PackedScene" uid="uid://raptor0shuriken01" path="res://scenes/projectiles/HateShuriken.tscn" id="12_shuriken"]
[ext_resource type="Script" path="res://src/World/CheckpointManager.cs" id="13_checkpointmgr"]
; ── Slice 11 additions — terrain textures for ParallaxBackground ─────────────
[ext_resource type="Texture2D" path="res://assets/sprites/terrain/sky_bg.png" id="22_skytex"]
[ext_resource type="Texture2D" path="res://assets/sprites/terrain/mid_land.png" id="23_midtex"]
[ext_resource type="Texture2D" path="res://assets/sprites/terrain/ground_land.png" id="24_groundtex"]
; ── Slice 9 additions ─────────────────────────────────────────────────────────
[ext_resource type="PackedScene" uid="uid://raptor0missile01" path="res://scenes/projectiles/Missile.tscn" id="21_missile"]
; ── Slice 8 additions ─────────────────────────────────────────────────────────
[ext_resource type="PackedScene" uid="uid://raptor0wraith01" path="res://scenes/enemies/WraithFighter.tscn" id="14_wraith"]
[ext_resource type="PackedScene" uid="uid://raptor0specter01" path="res://scenes/enemies/SpecterFighter.tscn" id="15_specter"]
; SporeCloud.tscn is baked into SpecterFighter.tscn — not needed here directly.
[ext_resource type="PackedScene" uid="uid://raptor0plasmablob01" path="res://scenes/projectiles/PlasmaBlob.tscn" id="17_plasmablob"]
[ext_resource type="PackedScene" uid="uid://raptor0crystalspine01" path="res://scenes/projectiles/CrystalSpine.tscn" id="18_crystalspine"]
[ext_resource type="PackedScene" uid="uid://raptor0organicspore01" path="res://scenes/projectiles/OrganicSpore.tscn" id="19_organicspore"]
[ext_resource type="PackedScene" uid="uid://raptor0harbinger01" path="res://scenes/enemies/HarbingerTurret.tscn" id="20_turret"]

; ── Scene tree ────────────────────────────────────────────────────────────────
; Slice 1 — camera + player.
; Slice 2 — ProjectilePool + PlasmaBolt wired.
; Slice 3 — one hard-placed enemy + score HUD.
; Slice 4 — scrolling camera, LevelDirector + EnemyContainer, wave JSON.
; Slice 5 — Boss.tscn wired to LevelDirector; spawns at t=162s.
; Slice 8 — WraithFighter/SpecterFighter wired; enemy projectiles slots 2–4;
;            4 HarbingerTurrets placed along level scroll path.
; Slice 9 — Missile.tscn wired to ProjectilePool slot [1].
; Slice 11— ParallaxBackground with sky/mid/ground layers; all Polygon2D
;            placeholders replaced with real sprites in subscene files.
;
; Required node paths (referenced by GetNode in C# _Ready() methods):
;   /root/Level01/ScrollCamera          ← Player.cs
;   /root/Level01/ProjectileContainer   ← PlasmaWeapon.cs, MissileWeapon.cs
;   /root/Level01/EnemyContainer        ← LevelDirector.cs
;   /root/Level01/Entities/Player       ← GameManager.cs (TODO comment)

[node name="Level01" type="Node2D"]

; ── Parallax Background (Slice 11) ────────────────────────────────────────────
; Three layers scrolling at different speeds create a depth illusion.
; motion_scale X drives how much of the camera's horizontal movement each layer follows:
;   Sky   0.08 — almost stationary, horizon effect.
;   Mid   0.35 — slow clouds / distant terrain.
;   Ground 1.00 — locks with the camera so the ground tiles stay fixed.
; mirror_x = true + a large Sprite2D tile the view horizontally without gaps.
[node name="ParallaxBackground" type="ParallaxBackground" parent="."]

[node name="SkyLayer" type="ParallaxLayer" parent="ParallaxBackground"]
motion_scale = Vector2(0.08, 0.0)
motion_mirroring = Vector2(1920, 0)

[node name="SkySprite" type="Sprite2D" parent="ParallaxBackground/SkyLayer"]
texture = ExtResource("22_skytex")
position = Vector2(960, 540)

[node name="MidLayer" type="ParallaxLayer" parent="ParallaxBackground"]
motion_scale = Vector2(0.35, 0.0)
motion_mirroring = Vector2(1920, 0)

[node name="MidSprite" type="Sprite2D" parent="ParallaxBackground/MidLayer"]
texture = ExtResource("23_midtex")
position = Vector2(960, 700)

[node name="GroundLayer" type="ParallaxLayer" parent="ParallaxBackground"]
motion_scale = Vector2(1.0, 0.0)
motion_mirroring = Vector2(1920, 0)

[node name="GroundSprite" type="Sprite2D" parent="ParallaxBackground/GroundLayer"]
texture = ExtResource("24_groundtex")
position = Vector2(960, 900)

; ── Camera ─────────────────────────────────────────────────────────────────────
; Centred at screen midpoint. ScrollCamera.cs calls MakeCurrent() in _Ready().
; Scrolls rightward at 120 px/s; stops when LevelDirector fires StopScroll at t=160s.
[node name="ScrollCamera" type="Camera2D" parent="."]
position = Vector2(960, 540)
script = ExtResource("1_scrollcam")

; ── Player ─────────────────────────────────────────────────────────────────────
[node name="Entities" type="Node2D" parent="."]

[node name="Player" parent="Entities" instance=ExtResource("2_player")]
position = Vector2(300, 540)

; ── Enemy container ─────────────────────────────────────────────────────────────
; LevelDirector.cs adds dynamically-spawned enemies here (ADR-004).
; Keeping enemies in their own container makes DespawnAllEnemies fast: iterate
; children rather than searching a mixed-content Entities node.
[node name="EnemyContainer" type="Node2D" parent="."]

; ── Projectile container ────────────────────────────────────────────────────────
; Required by PlasmaWeapon._Ready() and MissileWeapon._Ready() at scene load.
; All live projectiles are reparented here at spawn (ADR-003).
[node name="ProjectileContainer" type="Node2D" parent="."]

; ── Effects container ───────────────────────────────────────────────────────────
; Explosion VFX, SporeCloud instances. Empty until Slice 5.
[node name="EffectsContainer" type="Node2D" parent="."]

; ── HUD ────────────────────────────────────────────────────────────────────────
; Instanced from HUD.tscn (TICKET-704).  Owns score label, shield icon,
; life icons, missile ammo row, and boss health bar — all driven by EventBus.
[node name="HUD" parent="." instance=ExtResource("5_hud")]

; ── Projectile pool ─────────────────────────────────────────────────────────────
; NOT an autoload — lifetime is scoped to the level scene.
; ProjectileScenes array is indexed by ProjectileType enum value:
;   [0] PlasmaBolt  ← wired (Slice 2)
;   [1..7]          ← assigned in later slices; pool logs warnings for nulls
[node name="ProjectilePool" type="Node" parent="."]
script = ExtResource("3_pool")
; ProjectileScenes indexed by ProjectileType enum:
;   [0] PlasmaBolt         [1] Missile (Slice 9)  [2] PlasmaBlob
;   [3] CrystalSpine       [4] OrganicSpore        [5] ConstitutionBlast
;   [6] FoxBroadcastPulse                          [7] HateShuriken
ProjectileScenes = Array[PackedScene]([ExtResource("4_plasmabolt"), ExtResource("21_missile"), ExtResource("17_plasmablob"), ExtResource("18_crystalspine"), ExtResource("19_organicspore"), ExtResource("10_cblast"), ExtResource("11_foxpulse"), ExtResource("12_shuriken")])

; ── Checkpoints ─────────────────────────────────────────────────────────────────
; Three Marker2D nodes define respawn positions.  Indices match the JSON events:
;   0 = Checkpoint_Act1  (level start  — x=300,   never fired from JSON)
;   1 = Checkpoint_Act2  (t=80s        — x=9600,  scroll speed 120 px/s)
;   2 = Checkpoint_Boss  (t=160s       — x=19200, just before boss arena)
[node name="Checkpoints" type="Node2D" parent="."]

[node name="Checkpoint_Act1" type="Marker2D" parent="Checkpoints"]
position = Vector2(300, 540)

[node name="Checkpoint_Act2" type="Marker2D" parent="Checkpoints"]
position = Vector2(9600, 540)

[node name="Checkpoint_Boss" type="Marker2D" parent="Checkpoints"]
position = Vector2(19200, 540)

; CheckpointManager is NOT an autoload — lifetime is scoped to the level scene.
; Checkpoints array wired to the three Marker2D nodes above.
[node name="CheckpointManager" type="Node" parent="."]
script = ExtResource("13_checkpointmgr")
Checkpoints = Array[NodePath]([NodePath("../Checkpoints/Checkpoint_Act1"), NodePath("../Checkpoints/Checkpoint_Act2"), NodePath("../Checkpoints/Checkpoint_Boss")])

; ── Level Director ──────────────────────────────────────────────────────────────
; Reads res://data/level_01_waves.json and ticks the event timeline each frame.
; SpawnWave routes "enemy" param to the matching PackedScene export.
[node name="LevelDirector" type="Node" parent="."]
script = ExtResource("7_leveldirector")
BasicEnemyScene = ExtResource("6_basicenemy")
WraithFighterScene = ExtResource("14_wraith")
SpecterFighterScene = ExtResource("15_specter")
BossScene = ExtResource("8_boss")

; ── HarbingerTurrets (static editor placement) ───────────────────────────────
; 4 turrets spaced across the 19 200 px scroll path (~25s, ~60s, ~100s, ~140s).
; Turrets are NOT spawned by LevelDirector waves — they live here permanently.
; Y=980 places them near the bottom of the 1080-px viewport.
[node name="HarbingerTurret_1" parent="." instance=ExtResource("20_turret")]
position = Vector2(3000, 980)

[node name="HarbingerTurret_2" parent="." instance=ExtResource("20_turret")]
position = Vector2(7200, 980)

[node name="HarbingerTurret_3" parent="." instance=ExtResource("20_turret")]
position = Vector2(12000, 980)

[node name="HarbingerTurret_4" parent="." instance=ExtResource("20_turret")]
position = Vector2(16800, 980)
