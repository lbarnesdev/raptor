[gd_scene load_steps=4 format=3 uid="uid://ct10vlf38ik33"]

; ── External resources ────────────────────────────────────────────────────────
[ext_resource type="Script" path="res://src/Enemies/BaseEnemy.cs" id="1_enemy"]
[ext_resource type="PackedScene" uid="uid://raptor0explsmall01" path="res://scenes/fx/ExplosionSmall.tscn" id="2_expl"]
[ext_resource type="Texture2D" path="res://assets/sprites/enemies/wraith_fighter_idle.png" id="3_tex"]

; ── Collision shape ───────────────────────────────────────────────────────────
[sub_resource type="RectangleShape2D" id="1_enemyshape"]
size = Vector2(48, 32)

; ── Scene tree ────────────────────────────────────────────────────────────────
; Collision layers (project.godot layer names):
;   BasicEnemy — layer 4 (Enemy) = 2^3 = 8
;                mask 0  (stationary, no physics response needed)
;
; PlasmaBolt masks against layer 4 (value 8), so its Area2D.BodyEntered fires
; when a bolt overlaps this node.

[node name="BasicEnemy" type="CharacterBody2D"]
collision_layer = 8
collision_mask = 0
script = ExtResource("1_enemy")
ExplosionScene = ExtResource("2_expl")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("1_enemyshape")

; Sprite (Slice 11) — reuse WraithFighter art as a generic enemy stand-in.
[node name="Sprite2D" type="Sprite2D" parent="."]
texture = ExtResource("3_tex")
